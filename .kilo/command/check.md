---
description: Run lint and typecheck
---
Run `pnpm lint` then `pnpm exec tsc --noEmit` and report any errors with `file_path:line_number` references. Fix issues introduced by recent changes if obvious; otherwise list them.
