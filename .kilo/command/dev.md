---
description: Start Next.js dev server
---
Start the dev server with `pnpm dev` in the background. Wait until it logs the "Local:" URL, then return the URL. If port 3000 is busy, note which port is being used instead.